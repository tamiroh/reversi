{
    "name": "@reversi/core",
    "version": "0.1.0",
    "private": true,
    "type": "module",
    "exports": {
        ".": {
            "types": "./dist/index.d.ts",
            "import": "./dist/index.js"
        }
    },
    "scripts": {
        "build": "node scripts/clean-dist.mjs && tsc -p tsconfig.build.json",
        "test": "tsc -p tsconfig.json && node --test src/**/*.test.ts"
    },
    "devDependencies": {
        "@types/node": "^24.0.0",
        "typescript": "^5.8.0"
    }
}
